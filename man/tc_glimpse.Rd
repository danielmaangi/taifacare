% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glimpse.R
\name{tc_glimpse}
\alias{tc_glimpse}
\title{Glimpse a data frame}
\usage{
tc_glimpse(x, width = NULL, ...)
}
\arguments{
\item{x}{A data frame.}

\item{width}{Passed to \code{dplyr::glimpse()}.}

\item{...}{Additional arguments passed to \code{dplyr::glimpse()}.}
}
\value{
Invisibly returns \code{x} (same as \code{dplyr::glimpse()}).
}
\description{
Thin wrapper around \code{dplyr::glimpse()} for consistent \code{tc_}
naming. Operates on data already loaded into R (e.g. the result of
\code{\link{tc_query}} or one of the \code{tc_fetch_*} functions) — it
does not query ClickHouse itself.
}
\examples{
\dontrun{
  con <- tc_connect()
  df <- tc_query(con, "SELECT * FROM claim.claims_v2 LIMIT 100")
  tc_glimpse(df)
  DBI::dbDisconnect(con)
}

}
\seealso{
\code{\link{tc_query}}
}
