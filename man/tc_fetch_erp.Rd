% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_erp.R
\name{tc_fetch_erp}
\alias{tc_fetch_erp}
\title{Fetch ERP payment claims from ClickHouse}
\usage{
tc_fetch_erp(
  con,
  source = c("both", "old", "new"),
  payment_statuses = "paid",
  states = "done",
  old_table = "account_payable_claims_test",
  new_table = "ifs_test",
  schema = "erp"
)
}
\arguments{
\item{con}{A DBI connection, typically from \code{\link{tc_connect}}.}

\item{source}{Character scalar. Which source to fetch: \code{"both"}
(default), \code{"old"}, or \code{"new"}.}

\item{payment_statuses}{Character vector of payment statuses to include.
Default: \code{"paid"}.}

\item{states}{Character vector of claim states to include (old ERP only).
Default: \code{"done"}.}

\item{old_table}{Character scalar. Old ERP table name. Default:
\code{"account_payable_claims_test"}.}

\item{new_table}{Character scalar. New ERP (IFS) table name. Default:
\code{"ifs_test"}.}

\item{schema}{Character scalar. Schema containing both tables. Default:
\code{"erp"}.}
}
\value{
A data frame. When \code{source = "both"}, columns are
  \code{fid_code}, \code{fund_scheme}, \code{old_erp_amount}, and
  \code{new_erp_amount} (full join; NAs where a facility appears in only
  one source). When \code{source = "old"} or \code{"new"}, only the
  corresponding amount column is returned.
}
\description{
Queries old ERP, new ERP (IFS), or both claims tables and returns
aggregated claim amounts by facility and fund scheme. By default both
sources are fetched and joined on \code{fid_code} / \code{fund_scheme}.
}
\examples{
\dontrun{
  con <- tc_connect()

  erp <- tc_fetch_erp(con)                          # both sources
  erp <- tc_fetch_erp(con, source = "old")          # old ERP only
  erp <- tc_fetch_erp(con, source = "new", payment_statuses = "unpaid")
  erp <- tc_fetch_erp(con, states = c("done", "partial"))

  DBI::dbDisconnect(con)
}

}
\seealso{
\code{\link{tc_erp_summary}}
}
