% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_erp.R
\name{tc_fetch_old_erp}
\alias{tc_fetch_old_erp}
\title{Fetch old ERP payment claims from ClickHouse}
\usage{
tc_fetch_old_erp(
  con,
  states = "done",
  payment_statuses = "paid",
  schema = "erp",
  table = "account_payable_claims_test"
)
}
\arguments{
\item{con}{A DBI connection, typically from \code{\link{tc_connect}}.}

\item{states}{Character vector of claim states to include. Default:
\code{"done"}.}

\item{payment_statuses}{Character vector of payment statuses to include.
Default: \code{"paid"}.}

\item{schema}{Character scalar. Source schema name. Default: \code{"erp"}.}

\item{table}{Character scalar. Source table name. Default:
\code{"account_payable_claims_test"}.}
}
\value{
A data frame with columns \code{fid_code}, \code{fund_scheme}, and
  \code{old_erp_amount}.
}
\description{
Queries the old ERP claims table and returns aggregated claim amounts by
facility and fund scheme, filtered by claim state and payment status.
}
\examples{
\dontrun{
  con <- tc_connect()
  old_erp <- tc_fetch_old_erp(con)
  old_erp <- tc_fetch_old_erp(con, states = c("done", "partial"))
  DBI::dbDisconnect(con)
}

}
\seealso{
\code{\link{tc_fetch_new_erp}}, \code{\link{tc_erp_summary}}
}
