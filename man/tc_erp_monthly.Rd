% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{tc_erp_monthly}
\alias{tc_erp_monthly}
\title{Monthly paid ERP claims by fund scheme}
\usage{
tc_erp_monthly(
  con,
  source = c("both", "old", "new"),
  payment_statuses = "paid",
  states = "done",
  old_table = "account_payable_claims_test",
  new_table = "ifs_test",
  schema = "erp"
)
}
\arguments{
\item{con}{A DBI connection, typically from \code{\link{tc_connect}}.}

\item{source}{Character scalar. \code{"both"} (default), \code{"old"},
or \code{"new"}.}

\item{payment_statuses}{Character vector. Default: \code{"paid"}.}

\item{states}{Character vector. Claim states (old ERP only). Default:
\code{"done"}.}

\item{old_table}{Character scalar. Default:
\code{"account_payable_claims_test"}.}

\item{new_table}{Character scalar. Default: \code{"ifs_test"}.}

\item{schema}{Character scalar. Default: \code{"erp"}.}
}
\value{
A data frame with one row per month. Columns: \code{month},
  one column per fund scheme (0 where no claims), and \code{total}.
  Arranged by \code{month} ascending.
}
\description{
Queries old ERP, new ERP (IFS), or both and returns a wide table of
total paid claim amounts by calendar month and fund scheme, with a
grand total column. Old ERP dates use \code{ceo_approval_date}; new ERP
dates use \code{payment_date}.
}
\examples{
\dontrun{
  con <- tc_connect()
  tc_erp_monthly(con)
  tc_erp_monthly(con, source = "new")
  DBI::dbDisconnect(con)
}

}
\seealso{
\code{\link{tc_fetch_erp}}, \code{\link{tc_erp_summary}}
}
