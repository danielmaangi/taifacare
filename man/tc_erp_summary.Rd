% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{tc_erp_summary}
\alias{tc_erp_summary}
\title{Summarise ERP claim amounts by fund scheme}
\usage{
tc_erp_summary(data, amount_col, group_col = "fund_scheme")
}
\arguments{
\item{data}{A data frame, typically from \code{\link{tc_fetch_old_erp}} or
\code{\link{tc_fetch_new_erp}}.}

\item{amount_col}{Character scalar. Name of the column to sum.}

\item{group_col}{Character scalar or vector. Grouping column(s). Default:
\code{"fund_scheme"}.}
}
\value{
An ungrouped data frame with one row per group and the summed
  amount in a column with the same name as \code{amount_col}.
}
\description{
Groups a data frame by one or more columns and sums an amount column,
returning one row per group. Works with both old-ERP and new-ERP data
frames returned by the \code{tc_fetch_*()} functions.
}
\examples{
\dontrun{
  con <- tc_connect()
  old_erp <- tc_fetch_old_erp(con)
  tc_erp_summary(old_erp, "old_erp_amount")
  tc_erp_summary(old_erp, "old_erp_amount", group_col = c("county", "fund_scheme"))
  DBI::dbDisconnect(con)
}

}
